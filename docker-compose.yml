version: '3'

services:
  slc-roboticas:
    restart: always
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    command: >
      sh -c "python manage.py runserver 0.0.0.0:3000"
    pull_policy: build
